<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Moola Wallet — ENIGMA 💰</title>

  <!-- ===== PWA & FIREBASE ===== -->
  <link rel="manifest" href="manifest.json">
  <meta name="theme-color" content="#f59e0b">
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-firestore-compat.js"></script>

  <!-- ===== STYLES — Same Cosmic System ===== -->
  <link rel="stylesheet" href="style.css" />
  <style>
    /* Wallet-specific styles */
    .page-header {
      position: relative;
      z-index: 2;
      text-align: center;
      padding: 2rem 1rem 1rem;
    }

    .page-title {
      font-size: 1.75rem;
      font-weight: 800;
      background: linear-gradient(135deg, #fbbf24, #f59e0b, #d97706);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 0.25rem;
    }

    .balance-card {
      position: relative;
      z-index: 2;
      max-width: 420px;
      margin: 0 auto 1.5rem;
      background: linear-gradient(135deg, rgba(251,191,36,0.15), rgba(245,158,11,0.08));
      border: 1px solid rgba(251,191,36,0.35);
      border-radius: 24px;
      padding: 2rem 1.5rem;
      text-align: center;
      box-shadow: 0 8px 40px rgba(251,191,36,0.15), inset 0 0 30px rgba(251,191,36,0.05);
    }

    .balance-label {
      font-size: 0.8rem;
      color: rgba(255,255,255,0.5);
      text-transform: uppercase;
      letter-spacing: 0.15em;
      margin-bottom: 0.5rem;
    }

    .balance-display {
      font-size: 3rem;
      font-weight: 900;
      color: #fbbf24;
      text-shadow: 0 0 20px rgba(251,191,36,0.5);
      margin-bottom: 0.25rem;
    }

    .balance-note {
      font-size: 0.75rem;
      color: rgba(255,255,255,0.4);
    }

    .action-tabs {
      position: relative;
      z-index: 2;
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      padding: 0 1rem;
      margin-bottom: 1.5rem;
      overflow-x: auto;
      scrollbar-width: none;
    }

    .action-tabs::-webkit-scrollbar { display: none; }

    .action-tab {
      flex-shrink: 0;
      padding: 0.75rem 1.25rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 14px;
      color: rgba(255,255,255,0.6);
      font-size: 0.85rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.25s ease;
    }

    .action-tab.active {
      background: rgba(251,191,36,0.15);
      border-color: rgba(251,191,36,0.4);
      color: #fbbf24;
      box-shadow: 0 0 15px rgba(251,191,36,0.2);
    }

    .content-card {
      position: relative;
      z-index: 2;
      max-width: 420px;
      margin: 0 auto 1.5rem;
      background: rgba(18,18,60,0.6);
      border: 1px solid rgba(251,191,36,0.2);
      border-radius: 20px;
      padding: 1.5rem;
      display: none;
      backdrop-filter: blur(12px);
    }

    .content-card.active {
      display: block;
      animation: fadeIn 0.4s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .card-title {
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: 1.25rem;
      color: #fbbf24;
    }

    .quick-packs {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
      margin-bottom: 1.5rem;
    }

    .pack-option {
      background: rgba(251,191,36,0.08);
      border: 1px solid rgba(251,191,36,0.2);
      border-radius: 12px;
      padding: 1rem;
      text-align: center;
      cursor: pointer;
      transition: all 0.25s ease;
    }

    .pack-option:hover, .pack-option.selected {
      border-color: #fbbf24;
      background: rgba(251,191,36,0.15);
      transform: scale(1.03);
    }

    .pack-moola {
      font-weight: 900;
      font-size: 1.1rem;
      color: #fbbf24;
    }

    .pack-price {
      font-size: 0.75rem;
      color: rgba(255,255,255,0.5);
      margin-top: 0.25rem;
    }

    .form-group {
      margin-bottom: 1rem;
    }

    .form-label {
      display: block;
      font-size: 0.8rem;
      color: rgba(255,255,255,0.6);
      margin-bottom: 0.4rem;
    }

    .form-input {
      width: 100%;
      background: rgba(10,10,40,0.6);
      border: 1px solid rgba(251,191,36,0.25);
      border-radius: 10px;
      padding: 0.75rem 1rem;
      color: #fff;
      font-size: 1rem;
      outline: none;
    }

    .form-input:focus {
      border-color: #fbbf24;
      box-shadow: 0 0 0 3px rgba(251,191,36,0.15);
    }

    .btn-gold {
      width: 100%;
      background: linear-gradient(135deg, #f59e0b, #d97706);
      border: none;
      border-radius: 12px;
      padding: 0.9rem;
      color: #fff;
      font-size: 1rem;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 20px rgba(245,158,11,0.35);
    }

    .btn-gold:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 30px rgba(245,158,11,0.5);
    }

    .btn-gold:disabled {
      opacity: 0.6;
      transform: none;
      cursor: not-allowed;
    }

    .method-list {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin-bottom: 1.25rem;
    }

    .method-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.85rem 1rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.25s ease;
    }

    .method-item:hover, .method-item.selected {
      border-color: #fbbf24;
      background: rgba(251,191,36,0.08);
    }

    .method-icon { font-size: 1.2rem; }
    .method-name { font-weight: 600; font-size: 0.9rem; }
    .method-desc { font-size: 0.7rem; color: rgba(255,255,255,0.4); margin-top: 0.1rem; }

    .history-list {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .tx-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.85rem 0;
      border-bottom: 1px solid rgba(255,255,255,0.06);
    }

    .tx-info { display: flex; flex-direction: column; }
    .tx-title { font-weight: 600; font-size: 0.85rem; }
    .tx-date { font-size: 0.7rem; color: rgba(255,255,255,0.4); margin-top: 0.15rem; }
    .tx-amount { font-weight: 700; font-size: 0.9rem; }
    .tx-amount.plus { color: #34d399; }
    .tx-amount.minus { color: #f87171; }
  </style>
</head>

<body>
  <!-- 🌟 Same Cosmic Background -->
  <div class="space-bg">
    <div class="stars"></div>
    <div class="nebula"></div>
  </div>

  <!-- 💎 Balance Display -->
  <header class="page-header">
    <h1 class="page-title">Moola Wallet 💰</h1>
    <p class="page-subtitle">Your ENIGMA currency — earn, spend, send ✨</p>
  </header>

  <div class="balance-card">
    <div class="balance-label">Your Balance</div>
    <div class="balance-display" id="mainBalance">100</div>
    <div class="balance-note">1 Moola ≈ R1.00 ZAR • Global Gateway currency</div>
  </div>

  <!-- 🔄 Action Tabs -->
  <nav class="action-tabs">
    <div class="action-tab active" data-action="topup">⬆️ Top Up</div>
    <div class="action-tab" data-action="send">💸 Send</div>
    <div class="action-tab" data-action="withdraw">🏦 Cash Out</div>
    <div class="action-tab" data-action="history">📜 History</div>
  </nav>

  <!-- ⬆️ TOP UP Content -->
  <main class="content-card active" id="topupCard">
    <h3 class="card-title">Buy Moola — Quick Packs</h3>
    
    <div class="quick-packs">
      <div class="pack-option" data-moola="200" data-price="20">
        <div class="pack-moola">200 M</div>
        <div class="pack-price">R20</div>
      </div>
      <div class="pack-option" data-moola="300" data-price="30">
        <div class="pack-moola">300 M</div>
        <div class="pack-price">R30</div>
      </div>
      <div class="pack-option" data-moola="500" data-price="50">
        <div class="pack-moola">500 M</div>
        <div class="pack-price">R50</div>
      </div>
      <div class="pack-option" data-moola="1000" data-price="100">
        <div class="pack-moola">1,000 M</div>
        <div class="pack-price">R100</div>
      </div>
      <div class="pack-option" data-moola="2000" data-price="200">
        <div class="pack-moola">2,000 M</div>
        <div class="pack-price">R200</div>
      </div>
      <div class="pack-option" data-moola="5000" data-price="500">
        <div class="pack-moola">5,000 M</div>
        <div class="pack-price">R500</div>
      </div>
    </div>

    <h4 style="margin: 1rem 0 0.75rem; font-size: 0.9rem; color: rgba(255,255,255,0.6);">Payment Method</h4>
    
    <div class="method-list">
      <div class="method-item selected" data-method="eft">
        <span class="method-icon">🏦</span>
        <div>
          <div class="method-name">EFT / Bank Transfer</div>
          <div class="method-desc">Instant — all major SA banks</div>
        </div>
      </div>
      <div class="method-item" data-method="voucher">
        <span class="method-icon">🎫</span>
        <div>
          <div class="method-name">Voucher (OTT / Kazang)</div>
          <div class="method-desc">Buy at any store — enter code</div>
        </div>
      </div>
      <div class="method-item" data-method="airtime">
        <span class="method-icon">📱</span>
        <div>
          <div class="method-name">Convert Airtime</div>
          <div class="method-desc">MTN · Vodacom · Cell C</div>
        </div>
      </div>
      <div class="method-item" data-method="card">
        <span class="method-icon">💳</span>
        <div>
          <div class="method-name">Debit / Credit Card</div>
          <div class="method-desc">Visa · Mastercard</div>
        </div>
      </div>
      <div class="method-item" data-method="international">
        <span class="method-icon">🌍</span>
        <div>
          <div class="method-name">International Payment</div>
          <div class="method-desc">USD · GBP · EUR — PayPal / Stripe</div>
        </div>
      </div>
    </div>

    <div class="form-group" id="voucherInput" style="display: none; margin-top: 1rem;">
      <label class="form-label">Enter Voucher Code</label>
      <input type="text" class="form-input" id="voucherCode" placeholder="e.g. ENIGMA-XXXX-XXXX" />
    </div>

    <button class="btn-gold" id="topupBtn" style="margin-top: 1.5rem;">Top Up Now ✨</button>
  </main>

  <!-- 💸 SEND Content -->
  <main class="content-card" id="sendCard">
    <h3 class="card-title">Send Moola 💸</h3>
    
    <div class="form-group">
      <label class="form-label">Recipient Name or Email</label>
      <input type="text" class="form-input" id="sendTo" placeholder="Who are you sending to?" required />
    </div>
    
    <div class="form-group">
      <label class="form-label">Amount (Moola)</label>
      <input type="number" class="form-input" id="sendAmount" min="1" placeholder="How much?" required />
    </div>
    
    <div class="form-group">
      <label class="form-label">Message (Optional)</label>
      <input type="text" class="form-input" id="sendNote" placeholder="Add a little note..." />
    </div>
    
    <p style="font-size: 0.75rem; color: rgba(255,255,255,0.4); margin: -0.5rem 0 1rem;">
      ✅ Send fee: 0 Moola — completely free!
    </p>
    
    <button class="btn-gold" id="sendBtn">Send Now ✨</button>
  </main>

  <!-- 🏦 WITHDRAW / CASH OUT Content -->
  <main class="content-card" id="withdrawCard">
    <h3 class="card-title">Cash Out 🏦</h3>
    
    <div class="form-group">
      <label class="form-label">Amount (Moola)</label>
      <input type="number" class="form-input" id="withdrawAmount" min="200" placeholder="Min: 200 Moola" required />
    </div>
    
    <div class="form-group">
      <label class="form-label">Payment Method</label>
      <select class="form-input" id="payoutMethod">
        <option value="eft">Bank EFT (1–2 business days)</option>
        <option value="airtime">Airtime (Instant — MTN/Vodacom)</option>
        <option value="voucher">Retail Voucher (Send via SMS)</option>
        <option value="international">International — PayPal / Wise</option>
      </select>
    </div>
    
    <div class="form-group">
      <label class="form-label">Your Details</label>
      <input type="text" class="form-input" id="payoutDetails" placeholder="Account number / Phone / Email" required />
    </div>
    
    <div id="payoutRate" style="background: rgba(52,211,153,0.08); border-radius: 10px; padding: 0.75rem; margin: 1rem 0; font-size: 0.8rem;">
      <strong>Exchange Rate:</strong><br>
      <span id="rateText">200 Moola = R20 • 500 Moola = R50 • 1,000 Moola = R100</span>
    </div>
    
    <p style="font-size: 0.7rem; color: rgba(255,255,255,0.4); margin-bottom: 1rem;">
      ⚠️ Cash Out fee: 10% • Minimum: 200 Moola<br>
      International: 15% fee + currency conversion
    </p>
    
    <button class="btn-gold" id="withdrawBtn">Request Payout 🚀</button>
  </main>

  <!-- 📜 HISTORY Content -->
  <main class="content-card" id="historyCard">
    <h3 class="card-title">Transaction History 📜</h3>
    
    <div class="history-list" id="txHistory">
      <div class="tx-item">
        <div class="tx-info">
          <span class="tx-title">Welcome Bonus</span>
          <span class="tx-date">Just now</span>
        </div>
        <span class="tx-amount plus">+100</span>
      </div>
      <div class="tx-item" style="opacity: 0.6;">
        <div class="tx-info">
          <span class="tx-title">Entered Cape Town Chat</span>
          <span class="tx-date">—</span>
        </div>
        <span class="tx-amount minus">-3</span>
      </div>
      <div class="tx-item" style="opacity: 0.6;">
        <div class="tx-info">
          <span class="tx-title">Entered Global English Gateway</span>
          <span class="tx-date">—</span>
        </div>
        <span class="tx-amount minus">-10</span>
      </div>
    </div>
    
    <p style="text-align: center; margin-top: 1.5rem; font-size: 0.75rem; color: rgba(255,255,255,0.4);">
      Full history available once linked ✨
    </p>
  </main>

  <!-- 💙 Bottom Navigation -->
  <nav class="bottom-nav">
    <a href="index.html" class="nav-item">
      <span class="nav-icon">🏠</span>
      <span class="nav-label">Home</span>
    </a>
    <a href="chatrooms.html" class="nav-item">
      <span class="nav-icon">💬</span>
      <span class="nav-label">Chat</span>
    </a>
    <a href="social.html" class="nav-item">
      <span class="nav-icon">📸</span>
      <span class="nav-label">Social</span>
    </a>
    <a href="wallet.html" class="nav-item active">
      <span class="nav-icon">💰</span>
      <span class="nav-label">Moola</span>
    </a>
    <a href="profile.html" class="nav-item">
      <span class="nav-icon">👤</span>
      <span class="nav-label">Me</span>
    </a>
  </nav>

  <!-- ===== FIREBASE & WALLET LOGIC ===== -->
  <script>
    const firebaseConfig = {
      apiKey: "AIzaSyAfBOVVTaZuWdlwG-Yd6vI5hOpQzmhzjXk",
      authDomain: "enigma-42e91.firebaseapp.com",
      databaseURL: "https://enigma-42e91-default-rtdb.firebaseio.com",
      projectId: "enigma-42e91",
      storageBucket: "enigma-42e91.firebasestorage.app",
      messagingSenderId: "539411208616",
      appId: "1:539411208616:web:16f1f466166a5a5e29e59d",
      measurementId: "G-PN66LVMMW8"
    };

    if (!firebase.apps.length) firebase.initializeApp(firebaseConfig);
    const auth = firebase.auth();
    const db = firebase.firestore();

    // ===== Wallet State =====
    let userMoola = 100;
    let currentUser = null;
    let selectedPack = null;
    let selectedMethod = 'eft';

    // ===== Load User =====
    function loadUserData() {
      const saved = localStorage.getItem('enigma_user');
      if (saved) {
        currentUser = JSON.parse(saved);
        userMoola = currentUser.moola || 100;
        updateBalance();
      }
    }

    function updateBalance() {
      const el = document.getElementById('mainBalance');
      if (el) el.textContent = userMoola;
    }

    function saveUserData() {
      if (currentUser) {
        currentUser.moola = userMoola;
        localStorage.setItem('enigma_user', JSON.stringify(currentUser));
        if (currentUser.uid) {
          db.collection('users').doc(currentUser.uid).update({
            moola: userMoola
          }).catch(() => {});
        }
      }
    }

    // ===== Action Tabs =====
    const tabs = document.querySelectorAll('.action-tab');
    const cards = document.querySelectorAll('.content-card');

    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        tabs.forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
        const action = tab.dataset.action;
        cards.forEach(c => c.classList.remove('active'));
        document.getElementById(`${action}Card`).classList.add('active');
      });
    });

    // ===== Quick Pack Selection =====
    const packs = document.querySelectorAll('.pack-option');
    packs.forEach(pack => {
      pack.addEventListener('click', () => {
        packs.forEach(p => p.classList.remove('selected'));
        pack.classList.add('selected');
        selectedPack = {
          moola: parseInt(pack.dataset.moola),
          price: parseFloat(pack.dataset.price)
        };
      });
    });

    // ===== Payment Method Selection =====
    const methods = document.querySelectorAll('.method-item');
    methods.forEach(method => {
      method.addEventListener('click', () => {
        methods.forEach(m => m.classList.remove('selected'));
        method.classList.add('selected');
        selectedMethod = method.dataset.method;
        const voucherInput = document.getElementById('voucherInput');
        voucherInput.style.display = selectedMethod === 'voucher' ? 'block' : 'none';
      });
    });

    // ===== Top Up =====
    document.getElementById('topupBtn').addEventListener('click', () => {
      if (!selectedPack) {
        alert('✨ Please select a pack first!');
        return;
      }
      if (selectedMethod === 'voucher') {
        const code = document.getElementById('voucherCode').value.trim();
        if (!code) {
          alert('🎫 Please enter your voucher code!');
          return;
        }
      }
      
      userMoola += selectedPack.moola;
      saveUserData();
      alert(`✅ Success!\n+${selectedPack.moola} Moola added!\nNew Balance: ${userMoola} M\n\nThank you for supporting ENIGMA! 💜✨`);
      selectedPack = null;
      packs.forEach(p => p.classList.remove('selected'));
    });

    // ===== Send =====
    document.getElementById('sendBtn').addEventListener('click', () => {
      const to = document.getElementById('sendTo').value.trim();
      const amount = parseInt(document.getElementById('sendAmount').value);
      const note = document.getElementById('sendNote').value.trim() || 'No message';
      
      if (!to || !amount) {
        alert('✨ Please fill in all fields!');
        return;
      }
      if (amount > userMoola) {
        alert(`💎 Not enough Moola!\nYou have: ${userMoola}\nNeed: ${amount}`);
        return;
      }
      if (amount < 1) {
        alert('✨ Minimum send is 1 Moola!');
        return;
      }
      
      userMoola -= amount;
      saveUserData();
      alert(`✅ Sent!\n${amount} Moola → ${to}\nNote: "${note}"\nBalance: ${userMoola} M`);
      document.getElementById('sendTo').value = '';
      document.getElementById('sendAmount').value = '';
      document.getElementById('sendNote').value = '';
    });

    // ===== Withdraw =====
    document.getElementById('withdrawBtn').addEventListener('click', () => {
      const amount = parseInt(document.getElementById('withdrawAmount').value);
      const method = document.getElementById('payoutMethod').value;
      const details = document.getElementById('payoutDetails').value.trim();
      
      if (!amount || !details) {
        alert('✨ Please fill in all fields!');
        return;
      }
      if (amount < 200) {
        alert('⚠️ Minimum cash out: 200 Moola');
        return;
      }
      if (amount > userMoola) {
        alert(`💎 Not enough Moola!\nYou have: ${userMoola}`);
        return;
      }
      
      const feePercent = method === 'international' ? 15 : 10;
      const fee = Math.floor(amount * feePercent / 100);
      const payout = amount - fee;
      const zarValue = payout / 10;
      
      if (!confirm(`💸 Cash Out Request:\n\nAmount: ${amount} M\nFee (${feePercent}%): -${fee} M\nYou get: ${payout} M = R${zarValue.toFixed(2)}\n\nMethod: ${method.toUpperCase()}\nDetails: ${details}\n\nSubmit request?`)) {
        return;
      }
      
      userMoola -= amount;
      saveUserData();
      alert(`✅ Request Submitted!\n\n${payout} Moola → R${zarValue.toFixed(2)}\nMethod: ${method}\n\nProcessing time:\n• Airtime: Within 1 hour\n• EFT: 1–2 business days\n• International: 2–5 days\n\nThank you! 💜`);
      document.getElementById('withdrawAmount').value = '';
      document.getElementById('payoutDetails').value = '';
    });

    // ===== Auth Check =====
    auth.onAuthStateChanged(user => {
      if (!user) {
        window.location.href = 'login.html';
      } else {
        loadUserData();
      }
    });
  </script>

  <!-- 🌟 Cosmic Animation Engine -->
  <script src="app.js"></script>

</body>
</html>

